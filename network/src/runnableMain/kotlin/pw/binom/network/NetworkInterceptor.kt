package pw.binom.network

/*
import kotlinx.coroutines.Runnable
import kotlin.coroutines.CoroutineContext

interface NetworkInterceptor : CoroutineContext.Element {
    companion object Key : CoroutineContext.Key<NetworkInterceptor>

    fun dispatch(context: CoroutineContext, block: Runnable)
    fun isDispatchNeeded(context: CoroutineContext): Boolean
}
*/
